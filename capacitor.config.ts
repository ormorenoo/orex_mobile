import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.disoft.orex',
  appName: 'Orex',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
