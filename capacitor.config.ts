import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.orex.orexmobile',
  appName: 'orexIonic',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
